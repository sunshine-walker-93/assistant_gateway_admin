# Gateway Admin Service Environment Variables

# Database connection string (required)
# Format: user:password@tcp(host:port)/database?parseTime=true
# For Docker: use container name as host (e.g., assistant-mysql)
# For local: use 127.0.0.1 or localhost
ADMIN_DB_DSN=assistant:123456@tcp(assistant-mysql:3306)/assistant_gateway_db?parseTime=true

# HTTP listen address (optional, default: :8081)
ADMIN_HTTP_LISTEN=:8081

# MySQL credentials (used in docker-compose)
MYSQL_ROOT_PASSWORD=123456
MYSQL_USER=assistant
MYSQL_PASSWORD=123456
